#include "../headers/minitalk.h"



int main(void)
{

	printf("porcess id :%d",getpid());//change to ft_printf or put str

	//01000001 ==> A
	// handeler(SIGUSR2);
	// handeler(SIGUSR1);
	// handeler(SIGUSR2);
	// handeler(SIGUSR2);
	// handeler(SIGUSR2);
	// handeler(SIGUSR2);
	// handeler(SIGUSR2);
	// handeler(SIGUSR1);
	// while(1)
	// {
	// }
}

